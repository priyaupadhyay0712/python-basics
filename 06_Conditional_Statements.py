#Conditional statements(if,else)
#Comman error here is ** IndentationError **
#Pass OR Fail

avg = float(input("Enter average marks: ")

            if avg >= 40:            #In Python, indentation is mandatory and used to define code blocks.Here (:) is used to define the function and is signal the start of an indented bloack on the next line.
               print("PASSED")
            else:
               print("FAILED")
